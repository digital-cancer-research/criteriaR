% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evalTrialLogic.R
\name{evalTrialLogic}
\alias{evalTrialLogic}
\title{Evaluate Trial Eligibility Logic for a Patient}
\usage{
evalTrialLogic(patient, trial_logic_string)
}
\arguments{
\item{patient}{A list or data frame row representing a patient. Must contain fields \code{condition} and \code{alteration}.}

\item{trial_logic_string}{A character string representing R code that defines eligibility logic,
typically involving \code{condition} and \code{alteration}.}
}
\value{
Logical (\code{TRUE} or \code{FALSE}) indicating whether the patient satisfies the trial logic.
}
\description{
Parses and evaluates a trial eligibility logic string in the context of a given patient's clinical data.
}
\details{
This function takes a \code{patient} object (typically a list or data frame row)
containing at least \code{condition} and \code{alteration} fields, and a string representing
trial eligibility logic.\cr
The \code{trial_logic_string} is parsed and evaluated dynamically using \code{eval()} and \code{parse()},
enabling flexible trial matching based on complex logical criteria.\cr

\strong{Note:} As \code{eval(parse())} executes arbitrary R code, ensure that \code{trial_logic_string} is \strong{fully validated and controlled to avoid security risks.}
}
\examples{
# Example patient data
patient <- list(
  condition = "Lung Cancer",
  alteration = "EGFR Mutation"
)

# Example trial logic string
trial_logic_string <- "condition == 'Lung Cancer' && alteration == 'EGFR Mutation'"

# Evaluate trial eligibility
is_eligible <- evalTrialLogic(patient, trial_logic_string)

print(is_eligible)
#> TRUE

}
\seealso{
\code{\link[base:eval]{base::eval()}}, \code{\link[base:parse]{base::parse()}}
}
